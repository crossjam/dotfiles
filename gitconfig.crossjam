# -*- mode: conf; -*-

[user]
	name = "C. Ross Jam"
	email = "bmd@crossjam.net"

# Change SOME_SSH_KEY to a private key that has github access rights for the user crossjam
[core]
	sshCommand = "ssh -i ~/.ssh/crossjam_dotfiles.pub -o IdentitiesOnly=yes -F /dev/null"
	